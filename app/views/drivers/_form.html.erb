<%= form_for(@driver) do |f| %>
  <p><%= f.text_field :name, placeholder: "name" %></p>
  <p><%= f.text_field :license_plate, placeholder: "license plate" %></p>
  <p><%= f.email_field :email, placeholder: "email" %></p>
  <p><%= f.submit %></p>
<% end %>
