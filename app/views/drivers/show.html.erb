<h1><%= @driver.name %></h1>

<p>
  <%= link_to("edit profile", edit_driver_path(@driver)) %>
  |
  <%= link_to("delete profile", driver_path(@driver), method: :delete, data: {confirm: "Are you REALLY sure?"}) %>
</p>

<p>License plate: <strong><%= @driver.license_plate %></strong></p>

<p><%= mail_to(@driver.email) %></p>

<p><%= link_to("Back to drivers list", drivers_path) %></p>
